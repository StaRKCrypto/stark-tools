# Arcus S/R — paper desk notes

- Default posture: scan + log only (`ARMED=0` / `LIVE=0` in any real deploy)
- Public sample: zone detection on fake OHLCV — never posts orders
- Hosted product (gated): venue client + decision loop stay private
