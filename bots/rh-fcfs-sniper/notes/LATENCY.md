# RH FCFS — latency notes (teaser)

Illustrative product story only.

- FCFS races favor low-latency regions near chain infra
- Hosted idea: early timer + warm sockets + explicit arm — never auto-mint from scan
- Public sample: fake alert text only; no cookies, no keys, no send
